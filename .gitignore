# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Python
__pycache__/
*.py[cod]
*$py.class

# Models
# (We want to keep .pkl files, so we DO NOT ignore them)

# Large raw datasets (too big for GitHub — train locally then commit .pkl files)
EV_Car_Brake_Telemetry_v3_FINAL.csv

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
